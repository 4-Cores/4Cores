name: Sync to personal repo 4cores on master push

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        

      - name: Push to destination repo
        run: |
          git config --global user.name "ALPHA-1503"
          git config --global user.email "arnaudv2003@gmail.com"
          git push 'https://x-access-token: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@github.com/ALPHA-1503/test-cloning.git' master --force
